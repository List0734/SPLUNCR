[package]
name = "framework"
version = "0.1.0"
edition = "2024"

[features]
std = ["dep:toml"]

[dependencies]
nalgebra = "0.34.1"
crossbeam = "0.8.4"
serde = { version = "1.0.228", features = ["derive"] }
toml = { version = "0.9.11", optional = true }